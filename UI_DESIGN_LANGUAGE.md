# 每日食物记录小程序 UI 设计风格与设计语言

## 1. 设计定位

本小程序的 UI 风格定位为“轻松、温暖、清爽的个人饮食日记”。整体视觉参考提供的美食类移动端界面：浅色背景、圆角卡片、食物插画、青绿色主色、柔和阴影和高留白。

设计目标不是做成外卖或餐饮交易工具，而是做成一个让用户愿意每天打开、轻松记录、愉快回顾的食物记录工具。

关键词：

- 清爽。
- 温暖。
- 轻量。
- 有食欲。
- 日记感。
- 个人化。
- AI 友好但不科技感过重。

## 2. 视觉风格原则

### 2.1 轻背景

页面背景使用浅灰蓝或暖白色，避免纯白造成生硬感。

推荐：

- 主背景：`#F3F7F8`
- 次级背景：`#F8FAF7`
- 暖色背景块：`#FFF4DC`
- 卡片背景：`#FFFFFF`

页面整体应保持明亮、干净，适合展示食物图片和评分信息。

### 2.2 圆角卡片

参考图中卡片和弹窗都使用大圆角，给人轻松、亲和的感觉。

建议：

- 页面主卡片圆角：`24rpx` 到 `32rpx`
- 小卡片圆角：`20rpx`
- 按钮圆角：`999rpx`
- 图片圆角：`20rpx`
- 弹窗圆角：`32rpx`

食物记录卡片应像“小日记卡片”，不要像表格或后台管理列表。

### 2.3 柔和阴影

使用极轻的阴影强化层级，避免重阴影。

推荐：

```css
box-shadow: 0 12rpx 32rpx rgba(23, 42, 48, 0.06);
```

适用场景：

- 食物记录卡片。
- 日期总结卡片。
- AI 总结弹窗。
- 浮动添加按钮。

不建议给所有组件都加阴影，普通标签、输入框、分组项应保持扁平。

## 3. 色彩系统

### 3.1 主色

主色使用青绿色，参考图中按钮和价格文字的颜色。

| 用途 | 色值 | 说明 |
| --- | --- | --- |
| Primary | `#12B89F` | 主按钮、选中状态、重要操作 |
| Primary Dark | `#073B43` | 深色按钮、强调文字 |
| Primary Light | `#DDF7F1` | 选中标签背景、轻提示 |

### 3.2 辅助色

| 用途 | 色值 | 说明 |
| --- | --- | --- |
| Food Yellow | `#FFC857` | 星级评分、食物插画点缀 |
| Tomato Red | `#FF6B57` | 删除提醒、强提示、食物装饰 |
| Orange | `#FF9F43` | 高分、温暖提示 |
| Mint | `#BFEBDD` | 卡片插画背景 |
| Cream | `#FFF4DC` | 食物详情顶部背景 |

### 3.3 中性色

| 用途 | 色值 |
| --- | --- |
| 标题文字 | `#102A2F` |
| 正文文字 | `#334E52` |
| 次级文字 | `#7C8B8F` |
| 占位文字 | `#B8C3C6` |
| 分割线 | `#E8EEF0` |
| 禁用背景 | `#EEF3F4` |

## 4. 字体与排版

### 4.1 字体

微信小程序内优先使用系统字体。

```css
font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", Arial, sans-serif;
```

### 4.2 字号层级

| 层级 | 字号 | 字重 | 场景 |
| --- | --- | --- | --- |
| 页面大标题 | `40rpx` | 600 | 首页问候、详情页食物名称 |
| 区块标题 | `32rpx` | 600 | 今日记录、AI 总结、餐次分组 |
| 卡片标题 | `28rpx` | 600 | 食物名称 |
| 正文 | `26rpx` | 400 | 评价、备注 |
| 辅助文字 | `22rpx` | 400 | 日期、地点、标签说明 |
| 小标签 | `20rpx` | 500 | 餐次、标签、状态 |

### 4.3 排版风格

- 标题短句化，避免说明文字过长。
- 首页优先展示“今天吃了什么”和记录状态。
- 卡片内信息分层清晰：食物名 > 评分 > 评价 > 标签/时间。
- 不要在首页放过多说明文案，用户应直接开始记录。

## 5. 图形与插画语言

### 5.1 插画风格

参考图使用扁平美食插画，颜色明快，线条简单，适合作为本项目的主要视觉语言。

建议：

- 食物图片缺省图使用扁平插画。
- 空状态使用食物、餐盘、番茄、面包、饮品等元素。
- AI 总结可以使用小机器人、星星、便签、魔法笔等轻量图形，但不要过度科技化。

### 5.2 图片使用

食物记录可以同时支持真实照片和默认插画。

规则：

- 用户上传图片时优先显示真实图片。
- 无图片时使用餐次或食物类型插画占位。
- 图片不宜过暗，缩略图保持明亮。
- 图片容器统一圆角。

### 5.3 装饰元素

可使用少量食物碎片作为装饰，例如番茄、叶子、芝麻、小圆点。

使用限制：

- 只出现在首页顶部、详情页头图或空状态。
- 不应干扰文字阅读。
- 不应每个卡片都堆叠装饰。

## 6. 页面设计规范

## 6.1 首页：今日记录

首页应是用户打开小程序后的主操作页。

结构建议：

```text
顶部区域
  - 日期 / 今日状态
  - 问候语
  - 日期切换入口

今日概览卡片
  - 今日记录数量
  - 平均评分
  - AI 总结入口

餐次分组
  - 早餐
  - 午餐
  - 晚餐
  - 加餐

食物记录卡片列表
  - 食物图片
  - 食物名称
  - 评分
  - 简短评价
  - 标签

底部浮动添加按钮
```

视觉要求：

- 顶部保持充足留白，像参考图中的问候区域。
- 今日概览卡片可以使用浅青绿或浅奶油色背景。
- 食物记录卡片横向排列信息，避免拥挤。
- 无记录时显示插画空状态和一个主按钮。

首页文案示例：

- `今天吃了什么？`
- `记录一口好味道`
- `今日 3 条记录`
- `平均 4.3 分`

## 6.2 新增 / 编辑记录页

新增记录页应像填写一张轻量饮食便签。

字段布局顺序：

1. 食物图片。
2. 食物名称。
3. 日期。
4. 餐次。
5. 评分。
6. 标签。
7. 评价。
8. 地点、价格、备注等折叠或次级字段。

设计要求：

- 必填项要清晰，但不要用强烈红色制造压力。
- 评分使用星星或圆形分值按钮。
- 餐次使用横向胶囊按钮。
- 标签使用可多选 Chips。
- AI 生成评价入口放在评价输入框附近。

AI 按钮文案：

- `AI 帮我写评价`
- `推荐标签`
- `润色评价`

## 6.3 记录详情页

详情页可参考图中“食物详情”的布局：顶部大图或插画，中部显示食物名称和评分，下方展示评价、标签和元信息。

结构建议：

```text
顶部食物图片 / 插画
食物名称 + 日期
评分 + 餐次 + 时间
评价内容
标签列表
地点 / 价格 / 备注
编辑和删除操作
```

设计要求：

- 食物名称是视觉主角。
- 评分可用黄色星星突出。
- 用户评价使用独立白色卡片承载。
- 删除按钮弱化处理，避免误触。

## 6.4 AI 每日总结页 / 弹窗

AI 总结建议使用弹窗或独立卡片，参考图中的居中圆角弹窗。

结构建议：

```text
AI 插画
今日饮食总结
总结正文
建议列表
按钮：保存总结 / 重新生成
```

视觉要求：

- 弹窗背景白色，圆角大。
- 顶部可使用插画或评分星级。
- 主按钮使用深青色或青绿色。
- AI 内容必须允许用户修改或重新生成。

## 7. 组件规范

### 7.1 按钮

主按钮：

- 背景：`#12B89F`
- 文字：白色
- 高度：`88rpx`
- 圆角：`999rpx`
- 字重：600

深色按钮：

- 背景：`#073B43`
- 用于弹窗主操作或确认操作。

次级按钮：

- 背景：`#EEF8F6`
- 文字：`#12B89F`
- 用于 AI 生成、推荐标签、回到今天等操作。

危险按钮：

- 文字：`#FF6B57`
- 背景尽量透明或浅红，不使用大面积红色。

### 7.2 食物记录卡片

卡片字段：

- 图片缩略图。
- 食物名称。
- 评分。
- 餐次标签。
- 简短评价。
- 时间。

样式：

- 背景白色。
- 圆角 `28rpx`。
- 内边距 `24rpx`。
- 图片尺寸建议 `120rpx * 120rpx`。
- 卡片间距 `20rpx`。

### 7.3 标签 Chips

标签用于餐次、口味、AI 推荐等。

样式：

- 高度：`48rpx`
- 圆角：`999rpx`
- 内边距：`0 20rpx`
- 默认背景：`#F1F5F5`
- 选中背景：`#DDF7F1`
- 选中文字：`#0D9F8B`

### 7.4 评分组件

推荐第一版使用 1 到 5 星评分。

样式：

- 选中星星：`#FFC857`
- 未选中星星：`#DDE5E7`
- 支持点击星星改变评分。
- 显示评分文案，例如 `4 分 满意`。

评分文案：

- 1 分：`不喜欢`
- 2 分：`一般般`
- 3 分：`还可以`
- 4 分：`满意`
- 5 分：`超喜欢`

### 7.5 输入框

输入框应保持柔和、无强边框。

样式：

- 背景：`#F6F9F9`
- 圆角：`20rpx`
- 内边距：`24rpx`
- 占位文字：`#B8C3C6`
- 聚焦时边框：`2rpx solid #12B89F`

### 7.6 日期选择器

日期选择是核心功能，入口要明显。

建议形式：

- 首页顶部显示当前日期。
- 左右箭头切换前一天 / 后一天。
- 点击日期打开日期选择器。
- 提供 `今天` 快捷按钮。

选中日期状态：

- 背景：`#12B89F`
- 文字：白色
- 有记录的日期可用小圆点提示。

## 8. 交互语言

### 8.1 操作反馈

新增成功：

- `记录好了`

编辑成功：

- `已更新`

删除确认：

- `确定删除这条食物记录吗？`

AI 生成中：

- `正在整理今天的味道...`

AI 失败：

- `AI 暂时没生成成功，可以先手动记录`

### 8.2 空状态

无记录时不要只显示“暂无数据”，应更有引导感。

文案：

- `今天还没有记录`
- `从第一口开始记下来`
- 按钮：`添加食物`

视觉：

- 使用餐盘、便签、星星等插画。
- 背景保持浅色。

## 9. 动效建议

第一版可以使用轻量动效。

建议：

- 页面进入时卡片轻微上浮。
- 添加记录成功后按钮短暂反馈。
- 星级评分点击时有轻微缩放。
- AI 生成时使用小点加载或轻量插画动画。

避免：

- 大量复杂动画。
- 影响表单输入效率的动效。
- 闪烁或过强的颜色变化。

## 10. 微信小程序适配规范

### 10.1 设计尺寸

以 `750rpx` 宽度为基础设计。

常用间距：

- 页面左右边距：`32rpx`
- 区块上下间距：`32rpx`
- 卡片内边距：`24rpx`
- 卡片间距：`20rpx`
- 小元素间距：`12rpx`

### 10.2 底部安全区

浮动按钮和底部操作栏需要适配安全区。

建议：

```css
padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
```

### 10.3 点击区域

所有可点击元素最小点击区域不低于 `64rpx * 64rpx`。

## 11. 页面示例方向

### 11.1 首页视觉草图

```text
┌─────────────────────────┐
│  6月5日  周五       日历 │
│  今天吃了什么？          │
│  记录一口好味道          │
│                         │
│  ┌ 今日概览 ─────────┐  │
│  │ 3 条记录  平均 4.3 │  │
│  │ AI 总结            │  │
│  └───────────────────┘  │
│                         │
│  早餐                    │
│  [食物记录卡片]          │
│                         │
│  午餐                    │
│  [食物记录卡片]          │
│                         │
│              + 添加      │
└─────────────────────────┘
```

### 11.2 新增记录页视觉草图

```text
┌─────────────────────────┐
│  添加食物记录            │
│                         │
│  [ 上传图片 / 插画占位 ] │
│                         │
│  食物名称                │
│  [ 番茄牛腩饭           ]│
│                         │
│  日期     餐次           │
│  评分  ★ ★ ★ ★ ☆        │
│                         │
│  标签  清淡 辛辣 健康    │
│                         │
│  评价                    │
│  [ 写下今天的味道...     ]│
│  [ AI 帮我写评价 ]       │
│                         │
│  [ 保存记录 ]            │
└─────────────────────────┘
```

## 12. 与需求文档的对应关系

| 需求功能 | UI 表达方式 |
| --- | --- |
| 每日食物记录 | 首页按日期展示记录卡片 |
| 食物评价 | 新增页多行输入框、详情页评价卡片 |
| 食物打分 | 星级评分组件、平均评分概览 |
| 选择日期查看当天记录 | 顶部日期切换、日期选择器、回到今天按钮 |
| AI 生成评价 | 评价输入框旁的次级按钮 |
| AI 每日总结 | 今日概览卡片入口、弹窗或总结页 |
| 标签推荐 | Chips 多选标签、AI 推荐标签 |
| 编辑删除 | 详情页顶部或底部操作区 |

## 13. 设计禁忌

- 不要做成外卖下单界面，不突出购物车、价格和购买按钮。
- 不要使用过多深色背景，饮食记录应保持轻松明亮。
- 不要在首页堆叠太多统计图，第一屏应服务记录。
- 不要让 AI 功能抢占主流程，AI 是辅助，不是必填。
- 不要使用强医疗风格，例如医院蓝、体检表格、诊断提示。
- 不要使用过多渐变和高饱和背景，以免食物图片失去重点。

## 14. 第一版 UI 优先级

优先完成：

- 首页今日记录。
- 日期切换。
- 食物记录卡片。
- 新增记录表单。
- 星级评分。
- 标签 Chips。
- 空状态。

第二阶段完成：

- 详情页视觉优化。
- AI 总结弹窗。
- 月历视图。
- 统计页。
- 食物插画占位图库。

