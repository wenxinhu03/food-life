# 昏星味道日记下一阶段开发计划

## 1. 当前版本状态

当前小程序已经完成本地 MVP：

- 按日期查看每日食物记录。
- 新增、编辑、删除食物记录。
- 食物评分、标签、评价、地点、价格记录。
- 图片上传、裁剪展示、原图预览。
- 首页搜索食物名称，并可进入详情。
- 本地模拟 AI 评价草稿。
- 本地模拟 AI 每日总结。
- 数据使用微信本地缓存保存。

当前推荐调试基础库：

- `3.14.3`

暂不建议使用：

- `3.15.2`
- `3.16.1`

原因：当前开发环境中这两个基础库版本会触发开发者工具内部 `WAServiceMainContext timeout`。

## 2. 阶段一：稳定性测试与体验修复

目标：确保当前 MVP 在真机上可稳定使用。

### 2.1 真机测试清单

- 打开小程序首页。
- 切换前一天、后一天。
- 使用日期选择器选择日期。
- 新增一条食物记录。
- 上传图片并裁剪。
- 点击图片预览原图。
- 填写自由价格文本。
- 选择评分。
- 选择多个标签。
- 使用 AI 草稿。
- 保存记录。
- 点击记录进入详情页。
- 编辑记录。
- 删除记录。
- 搜索食物名称。
- 关闭小程序后重新打开，确认记录仍存在。

### 2.2 优先修复项

- 修复真机上出现的布局挤压。
- 修复图片裁剪或预览失败的兼容问题。
- 修复输入框在不同机型上的键盘遮挡。
- 修复搜索结果过多时的滚动体验。
- 检查所有中文文案是否自然。

## 3. 阶段二：UI 视觉打磨

目标：让小程序更接近参考图的卡通美食风格。

### 3.1 首页优化

- 优化“昏星味道日记”品牌头部。
- 增强卡通食物主视觉。
- 优化今日概览卡片。
- 优化食物记录卡片的图片、评分、标签层级。
- 搜索框和搜索结果卡片做得更轻量。

### 3.2 添加页优化

- 图片上传区域增加更明确的裁剪提示。
- 表单分组更清晰。
- 评分交互增加轻微动效。
- 标签选择区域增加常用标签优先排序。

### 3.3 详情页优化

- 图片头图增强视觉重点。
- 评价卡片更像“日记便签”。
- 编辑、删除按钮层级更清晰。

## 4. 阶段三：数据结构升级

目标：为后续云开发和统计功能做准备。

### 4.1 数据字段优化

建议补充字段：

```js
{
  displayImage: '',
  originalImage: '',
  tasteScore: 0,
  repeatIntent: '',
  source: 'manual',
  restaurantName: '',
  createdFromVersion: ''
}
```

### 4.2 本地数据迁移

- 为本地缓存增加版本号。
- 启动时检测旧数据。
- 自动补齐缺失字段。
- 避免后续字段升级导致旧记录报错。

## 5. 阶段四：统计功能

目标：让用户能看到自己的饮食偏好。

### 5.1 MVP 统计

- 最近 7 天记录数量。
- 最近 30 天平均评分。
- 最高评分食物。
- 最常吃食物。
- 最常用标签。

### 5.2 页面建议

新增页面：

```text
pages/statistics/statistics
```

展示模块：

- 总记录数。
- 平均评分。
- 常用标签。
- 高分食物列表。
- 最近记录趋势。

## 6. 阶段五：微信云开发

目标：让数据不再只保存在本地。

### 6.1 云开发能力

- 云数据库保存食物记录。
- 云存储保存图片。
- 云函数封装 AI 调用。
- 用户 OpenID 关联数据。

### 6.2 数据同步策略

第一版建议：

- 本地优先写入，提升速度。
- 保存成功后同步到云端。
- 云端失败时保留本地记录，并提示稍后同步。

### 6.3 云数据库集合

建议集合：

```text
food_records
daily_summaries
user_profiles
```

## 7. 阶段六：真实 AI 接入

目标：把当前本地模拟 AI 替换为真实 AI 能力。

### 7.1 AI 功能

- 根据食物名称、评分、标签生成评价草稿。
- 根据当天记录生成每日总结。
- 根据评价内容推荐标签。
- 根据图片识别食物名称。

### 7.2 技术方案

调用链路：

```text
小程序前端
  -> 云函数
  -> AI 服务
  -> 返回结果
  -> 前端展示并允许用户修改
```

注意：

- API Key 只能放在云函数环境变量。
- 前端不能暴露密钥。
- AI 结果必须允许用户编辑。
- 健康建议需要标注“仅供参考”。

## 8. 阶段七：发布准备

目标：准备体验版和正式上线。

### 8.1 基础信息

- 小程序名称。
- 小程序头像。
- 服务类目。
- 隐私协议。
- 用户数据说明。

### 8.2 发布前检查

- 固定可用基础库版本。
- 真机测试 iOS 和 Android。
- 检查图片上传权限。
- 检查本地缓存容量。
- 检查无网络状态下是否能正常记录。
- 检查删除操作是否有确认。

## 9. 推荐下一步

建议接下来优先做：

1. 完成一轮真机测试。
2. 修复测试中发现的交互问题。
3. 新增统计页。
4. 再决定是否接云开发。

当前阶段不建议立刻接真实 AI。先把记录体验和数据结构打稳，后续接 AI 会更顺。

